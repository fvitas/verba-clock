<?xml version="1.0" encoding="utf-8"?>
<!-- Two layers, so the letters never stretch: the finish gradient fills the frame (fitXY is
     harmless on a gradient), while the square letter bitmap is scaled with fitCenter, which
     preserves its aspect the way the web face's cqmin units do -->
<FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@drawable/widget_background"
    android:clipToOutline="true">

    <ImageView
        android:id="@+id/widget_surface"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:scaleType="fitXY"
        android:importantForAccessibility="no" />

    <ImageView
        android:id="@+id/widget_face"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:scaleType="fitCenter"
        android:contentDescription="@string/widget_description" />
</FrameLayout>
